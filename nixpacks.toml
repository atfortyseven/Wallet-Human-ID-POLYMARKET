[phases.setup]
nixPkgs = ["nodejs", "openssl"]

[phases.install]
cmds = ["npm install --prefer-offline --no-audit"]

[phases.build]
cmds = ["npx prisma generate", "next build"]

[start]
cmd = "npm start"
